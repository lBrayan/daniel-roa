name: Deploy Portfolio with CDK

on:
  push:
    branches:
      - main

concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node & pnpm
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install -g pnpm

      # 1. Instala dependencias de la raíz (Next.js)
      - name: Install Dependencies
        run: pnpm install

      - name: Build Next.js App
        run: pnpm build

      - name: 🔑 Configurar AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      # 2. ¡Recuperamos este paso vital para instalar CDK y tsx!
      - name: Install CDK Dependencies
        run: |
          cd infrastructure
          pnpm install

      # 3. Despliegue
      - name: CDK Deploy
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          cd infrastructure
          # Ahora el ejecutable local sí existe
          pnpm exec cdk deploy --require-approval never